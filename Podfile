# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'IgHashTag' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RealmSwift'
  pod 'FSPagerView'
  pod 'SDWebImage', '~> 4.0'
  pod 'Alamofire', '~> 4.7'

  # Pods for IgHashTag

  target 'IgHashTagTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'IgHashTagUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
